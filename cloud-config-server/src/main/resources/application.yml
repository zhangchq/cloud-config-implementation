security:
  basic:
    enabled: true
  user:
    name: cloud
    password: config
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhangchq/central-configurations
          search-paths: '{application}'